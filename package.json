{
  "name": "claim-center",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "Claim Center clean rewrite on Cloudflare Workers + D1",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "check": "node --check worker/main.js && node --check site/app.js",
    "test": "node -e \"const fs=require('fs');const h=fs.readFileSync('site/index.html','utf8');for(const v of ['RunValidation','data-page=\\\"validation\\\"','data-theme-choice','Master Article — Weekly Replace'])if(!h.includes(v))throw new Error('Missing clean UI contract: '+v);if(h.includes('Pivot Table'))throw new Error('Pivot Table must not exist in clean UI');console.log('clean rewrite contract ok')\"",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:remote": "wrangler d1 migrations apply claim-center --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
